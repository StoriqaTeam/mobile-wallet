// @flow

import {
  StyleSheet,
} from 'react-native';

export const commonStyles = StyleSheet.create({
  containerView: {
    backgroundColor: '#fff',
    height: '100%',
  },
  view: {
    marginTop: 30,
  },
  viewTitle: {
    fontSize: 36,
    fontWeight: 'bold',
  },
});

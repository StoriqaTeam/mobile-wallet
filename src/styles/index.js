// @flow

import {
  StyleSheet,
} from 'react-native';

export const commonStyles = StyleSheet.create({
  containerView: {
    backgroundColor: '#fff',
    height: '100%',
    display: 'flex',
  },
  view: {
    marginTop: 13,
  },
  viewTitle: {
    fontSize: 36,
    fontWeight: 'bold',
  },
});
